class NewsModel{
  String url;
  String title;
  String content;
  String description;
  String urlToImage;
  NewsModel({required this.url,required this.title,required this.content,required this.description,required this.urlToImage});
}